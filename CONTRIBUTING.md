# 参与贡献

先说一条底线，它比下面所有规则加起来都重要：

> **这个项目里任何一个来路不明的数字都是 bug。**
>
> 包括界面上的"预估"、报告里的"典型值"、和测试里为了让它过而硬填的期望值。

这是一个机械选型工具。它的全部价值在于每个数字可追溯。
一个查不到出处但"业内都这么用"的系数，写进去没人会质疑——
而这恰恰是最危险的：它在界面上和抄自国标的数长得一模一样，用户无从分辨。

---

## 环境

```bash
python -m venv .venv
.venv/Scripts/python.exe -m pip install -e . ".[dev]"   # Windows
.venv/Scripts/python.exe -m pytest -q                    # 497 项，应全绿

cd web && npm install                                    # 只在改前端时需要
```

需要 Python 3.10+。改前端另需 Node 18+；打安装包另需 Rust 与 MSVC 工具链。

---

## 最有价值的三类贡献

### 1. 补数据并完成双源核验 ★

**当前 25 张表，0 张完成双源核验**——这是整个项目最大的短板。

补一张表比加一个功能有用得多。做法：

```bash
cd engine
python -m mds audit            # 看缺口清单，精确到哪张表、哪个分组、哪一行
```

录入规则（不可商量的部分）：

- `data_source` **必填**，写到标准号 + 表号，或手册名 + 卷/篇/页
- 标 `verified` **必须**有 `second_source`，且不得与主信源相同
- **两处信源不一致的格子不录**，把冲突写进 `_todo`
- **查不到就留空**。审计会把缺口精确报出来，界面有「去补录」入口
- 凡是有定义式的（如 `As = 0.7854(d−0.9382P)²`），**写一条测试复算校验它**

最后一条最有用。建表时靠它抓到过四处转写错误，其中一处
（螺栓 M30 的应力截面积被抄成 581，正确 560.6）差 3.6%，
两处转载对不上时，公式直接判了胜负。

> **别把"业内通行值"当成可追溯。** 拿不到出处时有三条正路：
> 改由用户填（这个数印在厂商样本或材料证书上）、表里留空（报缺口）、
> 整列不录（转载本身不可信）。编一个，不在其中。

### 2. 新增物料

**不改代码，写一份 YAML。** 完整说明书在 [`docs/workflow-spec.md`](docs/workflow-spec.md)，
十一种步骤类型每种都有真实例子，末尾是加新物料的七步流程。

一个新物料的完整交付是四件：

| 文件 | 内容 |
|---|---|
| `engine/workflows/<m>.yaml` | 可执行规格，唯一的计算真源 |
| `engine/knowledge/cache/<m>/*.yaml` | 数据表，每张带 frontmatter |
| `engine/workflows/<m>.md` | 人读文档，按 `_template.md` 写 |
| `engine/tests/test_<m>.py` | 黄金算例 + 边界 |

**`.md` 里必须有「未实现项」一节**：凡是这个物料该做而没做的，
逐条写明缺什么数据才能做。不是删掉，是说清楚。

### 3. 修 bug、补测试、改文档

改动前请过一遍 [`docs/development.md`](docs/development.md) 末尾的自检清单。

---

## 改动时的自检

动核心逻辑前问自己：

- [ ] 破坏了三条硬规则吗？（拒绝外推 / AI 不碰计算 / 置信度是事实）
- [ ] 新增的失败模式，用户该做什么？是**补数据**还是**改设计**？选对状态了吗？
- [ ] 界面上"未执行"和"不通过"还分得开吗？
- [ ] 打包态会怎样？有按 `__file__` 定位资源吗？
- [ ] 导出报告里会不会漏进未渲染的模板变量？
- [ ] 加了新数字吗？**它的出处能追溯吗？能不能用定义式复算一遍？**
- [ ] 改了 `workflows/*.yaml` 吗？对应的 `.md` 回填了吗？
- [ ] 动了引导式通道吗？三道闸门（依据取证 + 公式里没有编出来的系数 +
      source.ref 落在已确认依据之内）还在吗？修正循环有没有悄悄放宽判据？

`DataMissing`（该补数据）和 `NoSolution`（该改设计）**必须分清**。
把两种不同的事情叫成一个名字，会让软件说出误导用户的话。

---

## 测试取向

重点不是覆盖率，是守住那些**破了就毁掉产品价值**的性质。
测试名本身就该说清守的是什么：

```
test_refuses_to_extrapolate_beyond_row_range
test_width_beyond_series_is_no_solution_not_missing_data
test_skipped_checks_are_not_reported_as_failures
test_gear_does_not_pretend_to_do_a_full_strength_calculation
```

**黄金用例的期望值必须是独立笔算的，不能是把引擎输出抄回来。**
改公式时重新手算，别直接改期望值。

**不要写"跟上次一样"的断言。** 写死张数、写死某个物料处于某个状态这类断言，
每加一个物料就要改一次数字，改着改着就只剩下"跟上次一样"的意思了。
改成守性质：比如断言汇总的表数与引擎实际持有的一致。

---

## 代码风格

- 注释写**为什么**，不写是什么。尤其是"看起来可以简化但不能简化"的地方
- 错误信息面向用户：说清发生了什么 + 该做什么，不要只抛类型名
- 中文用于所有面向用户的文本；代码标识符用英文
- 数值展示统一走 `mds.expr.fmt_num`，别在各处 `round`

---

## 已知的环境坑

这几个都实际踩过，会反复咬人，详见 [`docs/development.md`](docs/development.md)：

1. **PyYAML 的两段式文档** —— 缓存文件是 frontmatter + 正文两个 YAML 文档，
   必须 `safe_load_all` 再合并，`safe_load` 会抛 `ComposerError`
2. **YAML 里的嵌套引号** —— `_todo` 这类双引号字符串里再用 ASCII 双引号会解析失败，
   用中文引号「」（已踩三次）
3. **打包态的 `__file__`** —— PyInstaller 打包后指向归档内部，
   数据根靠 `MDS_SKILL_ROOT` 环境变量确定
4. **Windows PowerShell 5.1** —— `.ps1` 要存成带 BOM 的 UTF-8，
   否则中文按 ANSI 读成乱码；且它会把原生 exe 的 stderr 当致命错误

---

## 许可

提交贡献即表示你同意按 [Apache-2.0](LICENSE) 授权你的贡献。

如果你提交的是**数据**，请一并说明来源，并确认你有权以此方式分享它。
见 [DATA_NOTICE.md](DATA_NOTICE.md)。
